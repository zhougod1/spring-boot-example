<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sb.project.example.mapper.CorpMapper">
    <select id="queryCorpList" resultType="sb.project.example.entity.Corp">
        SELECT corp_id, city_id, corp_name  FROM corp  limit 0,10
    </select>
</mapper>
